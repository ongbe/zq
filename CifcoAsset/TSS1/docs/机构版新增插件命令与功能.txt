在fasttrader插件上需要增加的功能或命令字

1.  查询某个帐号下所有持仓（插件发起）
2.  查询某个帐号下所有持仓明细（插件发起）
3.  执行强制下单审核（插件发起，无附加数据）
4.  撤销强制下单审核（插件发起，无附加数据）
5.  FastTrader插件接口上需要保存当前强制下单审核状态即强制下单审核的窗体句柄。
6.  FastTrader插件接口上增加下单审核功能（附加数据，1个PlatformStru_InputOrder）。


7.  获得指定帐号、指定合约的开仓未成交手数。
8.  获得某个帐号下所有持仓明细(不包含数量为0的)的开仓时间，返回值持仓明细结构体（持仓明细的opendate改为开仓时间）（持仓明细时间可以去查找对应的成交，对应的成交的时间就是持仓明细开仓时间）。


进度：希望在下周1前全部完成。